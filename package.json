{
  "name": "wande-tricada-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "blog:thumbnail": "node scripts/create-blog-thumbnail.mjs",
    "blog:generate": "node scripts/generate-blog.mjs",
    "build": "node scripts/generate-blog.mjs && vite build && node scripts/generate-blog-pages.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "gh-pages": "latest"
  }
}
